'use client';

import React from 'react';
import { TopicExplorerTab } from './TopicExplorerTab';

/**
 * KnowledgeGraphTab now renders the DSA Topic Explorer.
 * Replaces the previous empty canvas with the comprehensive 3-level DAG curriculum roadmap.
 */
export function KnowledgeGraphTab() {
  return <TopicExplorerTab />;
}
